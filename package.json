{
  "name": "id-scanner",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "venv/Scripts/uvicorn.exe main:app --host 127.0.0.1 --port 8080 --reload",
    "deploy": "powershell -ExecutionPolicy Bypass -File ./deploy.ps1"
  }
}
